export const resetProblem = () => ({
    type: 'RESET_PROBLEM'
});

export const resetProblemState = () => ({
    type: 'RESET_PROBLEM_STATE'
});

export const setNumDigits = (numDigits) => ({
    type: 'SET_NUM_DIGITS',
    numDigits
});

export const setNumElements = (numElements) => ({
    type: 'SET_NUM_ELEMENTS',
    numElements
});